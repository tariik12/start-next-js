import type { Metadata } from 'next'
import React from 'react';
export const metadata: Metadata = {
    title: 'Profile || start next js',
    description: 'start next js',
  }
  

const page = () => {
    return (
        <div>
            <h1>Profile  Pages</h1>
        </div>
    );
};

export default page;